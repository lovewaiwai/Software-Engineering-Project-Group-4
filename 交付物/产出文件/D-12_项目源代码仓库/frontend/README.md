# SwapCampus Frontend

Vue 3 + Vite + TypeScript scaffold for SwapCampus.

```powershell
npm install
npm run dev
npm run build
```

Routes are defined in `src/router/index.ts`; placeholder pages live under `src/views/**` and should be expanded by module agents according to the D4-D5 API draft.
